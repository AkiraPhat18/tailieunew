#include <stdio.h>
#include <math.h>

int main() {
    double number = 16.0; // So ban muon tính can bac hai
    double squareRoot = sqrt(number);

    printf("Can bac hai cua %lf là %lf\n", number, squareRoot);

    return 0;
}

